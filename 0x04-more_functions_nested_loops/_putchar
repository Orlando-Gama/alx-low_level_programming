#include <unistd.h>

/**
  *_putchar - wtite the character c to stdout
  *@c:The character to print
  *Return 1 if true, 0 if false
  *on error, -1 return.
  */
int_putchar(char c)
{
	return (write(1, &c, 1));
}
